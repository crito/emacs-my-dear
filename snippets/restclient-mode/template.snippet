# -*- mode: snippet -*-
# name: template
# key: template
# --

# Request for ${1:project}

# Examples:
# # Some generic vars
# :user-id = 7
# :the-name := (format "%s %s %d" 'Neo (md5 "The Chosen") (+ 100 1))
#
# PUT http://localhost:4000/users/:user-id/
# Authorization: :my-auth
#
# { "name": ":the-name" }

$0